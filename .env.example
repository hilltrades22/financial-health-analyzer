# Required: SEC EDGAR requires a real, identifying contact User-Agent on
# every request (https://www.sec.gov/os/webmaster-faq#developers).
# Format: "<App/Company Name> <contact-email>"
SEC_USER_AGENT="Financial Health Analyzer contact@example.com"

# Port the server listens on (Render sets this automatically in production).
PORT=8000
